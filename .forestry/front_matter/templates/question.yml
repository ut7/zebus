---
label: Question
hide_body: true
fields:
- name: image
  type: file
  config:
    maxSize: 250
  label: Image
  description: L'image de la question
- name: question
  type: text
  config:
    required: false
  label: Question
  description: La question
- name: reponses
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Réponses
  description: Les réponses
- name: icone_reponse_1
  type: select
  default: ''
  config:
    required: false
    options:
    - cercle
    - carré
    - triangle
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Icône de la réponse 1
  description: L'icône devant la réponse 1
